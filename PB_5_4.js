function read (input) {
    let num = Number(input[0]);
    let count = 1;
    //let number = Number(input[index]); index++;
    //let sum2 = number;
    while (count <= num) {
        console.log(count);
        count = 2*count + 1;
        
    }
} read (["100"])
