function newYaerCount() {
    let h = 24;
    let m = 60;
    let s = 60;
    for (let i = 0; i < h; i++) {
        for (let p = 0; p < m; p++) {
            console.log(`${i}:${p}`);
            }
        }
    }
newYaerCount();
