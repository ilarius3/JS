function newYaerCount(input) {
    let n = Number(input[0])
    for (let i = 0; i <= n; i =i+2) {
	    //let number = Math.pow(2, i)		
	    console.log(Math.pow(2, i)) // console.log(number)
        }
    }
newYaerCount(["8"]);
