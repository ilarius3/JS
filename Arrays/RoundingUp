function solve(arr) {
    for (let i= 0; i<arr.length; i++) {
        let element = arr[i];
        element = Math.ceil(arr[i]);
        arr[i] = element
    }
    console.log(arr.join());
}
solve([0.5, 2.2, 3.3]);
