function sumFirstAndLast(arr){
    let firstElement = arr[0];
    let lastIndex = arr.length - 1;
    let lastElement = arr[lastIndex];
    console.log(firstElement + lastElement);
    
}
let myArr = [20, 30, 40]
sumFirstAndLast(myArr)
